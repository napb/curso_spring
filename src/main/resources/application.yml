server:
  error:
    whitelabel:
      enabled: false
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/udemy
    username: root
    password: 0000077
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      naming:
        strategy: org.hibernate.cf.ImprovedNameStrategy
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
